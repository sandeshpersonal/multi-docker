sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t sandesh1205/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true sandesh1205/react-test npm test

after_success:
  - docker build -t sandesh1205/multi-client ./client
  - docker build -t sandesh1205/multi-nginx ./nginx
  - docker build -t sandesh1205/multi-server ./server
  - docker build -t sandesh1205/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push sandesh1205/multi-client
  - docker push sandesh1205/multi-nginx
  - docker push sandesh1205/multi-server
  - docker push sandesh1205/multi-worker
